const assert = require('assert');
const app = require('../../app'); // Replace with the actual path to your application's main file.

describe('Your Application', function () {
  it('should do something', function () {
    // Your test code here
    const result = add(2,3);
    assert.equal(result, 5); // Replace with your actual test logic
  });
});
